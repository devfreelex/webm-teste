import styles from './appTabs.styles'
import template from './appTabs.template'

export default () => {

    const tagName = 'app-tabs'

    return {
        tagName,
        styles,
        template
    }
}