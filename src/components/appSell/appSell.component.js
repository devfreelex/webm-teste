import styles from './appSell.styles'
import template from './appSell.template'

export default () => {

    const tagName = 'app-sell'

    return {
        tagName,
        template,
        styles
    }
}